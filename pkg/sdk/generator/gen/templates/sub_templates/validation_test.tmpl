{{- /*gotype: github.com/Snowflake-Labs/terraform-provider-snowflake/pkg/sdk/generator/gen.Field*/ -}}

{{ $field := . }}
{{- range .Validations }}
    {{- if not .IsAdditionalValidations }}
    t.Run("{{ .TodoComment $field }}", func(t *testing.T) {
        opts := defaultOpts()
        // TODO: fill me
        assertOptsInvalidJoinedErrors(t, opts, {{ .ReturnedError $field }})
    })
    {{- end }}
{{ end -}}
