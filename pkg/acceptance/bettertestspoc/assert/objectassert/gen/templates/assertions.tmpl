{{- /*gotype: github.com/Snowflake-Labs/terraform-provider-snowflake/pkg/acceptance/bettertestspoc/assert/objectassert/gen.SnowflakeObjectAssertionsModel*/ -}}

{{ $assertName := .Name | printf "%sAssert" }}
{{ $sdkType := .SdkType }}
{{ $nameLowerCase := FirstLetterLowercase .Name }}
{{ $assertVar := FirstLetter $nameLowerCase }}
{{- range .Fields }}
{{- if not .SkipGeneration }}
{{- if .GenerateNestedAdapter }}
{{- if .IsOriginalTypeSlice }}
    {{ $itemType := TypeWithoutPointerAndBrackets .ConcreteType }}
    func ({{ $assertVar }} *{{ $assertName }}) Has{{ .Name }}With(subAsserts ...*{{ TrimPrefix $itemType "sdk." }}Assert) *{{ $assertName }} {
        {{ $assertVar }}.AddAssertion(func(t *testing.T, o *{{ $sdkType }}) error {
            if len(o.{{ .Name }}) != len(subAsserts) {
                return fmt.Errorf("expected %d {{ CamelToWords .Name }}; got: %d", len(subAsserts), len(o.{{ .Name }}))
            }
            return nil
        })
        for idx, subAssert := range subAsserts {
            idx, subAssert := idx, subAssert
            for _, assertion := range subAssert.GetAssertions() {
                assertion := assertion
                {{ $assertVar }}.AddAssertion(func(t *testing.T, o *{{ $sdkType }}) error {
                    if idx >= len(o.{{ .Name }}) {
                        return fmt.Errorf("expected at least %d {{ CamelToWords .Name }}; got: %d", idx+1, len(o.{{ .Name }}))
                    }
                    return assertion(t, &o.{{ .Name }}[idx])
                })
            }
        }
        return {{ $assertVar }}
    }
{{- else if .IsOriginalTypePointer }}
    func ({{ $assertVar }} *{{ $assertName }}) Has{{ .Name }}() *{{ $assertName }} {
        {{ $assertVar }}.AddAssertion(func(t *testing.T, o *{{ $sdkType }}) error {
            if o.{{ .Name }} == nil {
                return fmt.Errorf("expected {{ CamelToWords .Name }} to have value; got: nil")
            }
            return nil
        })
        return {{ $assertVar }}
    }

    func ({{ $assertVar }} *{{ $assertName }}) Has{{ .Name }}With(subAssert *{{ TrimPrefix (TypeWithoutPointerAndBrackets .ConcreteType) "sdk." }}Assert) *{{ $assertName }} {
        {{ $assertVar }}.Has{{ .Name }}()
        for _, assertion := range subAssert.GetAssertions() {
            assertion := assertion
            {{ $assertVar }}.AddAssertion(func(t *testing.T, o *{{ $sdkType }}) error {
                return assert.AssertionOnPointerField(t, o.{{ .Name }}, "{{ CamelToWords .Name }}", assertion)
            })
        }
        return {{ $assertVar }}
    }
{{- else }}
    func ({{ $assertVar }} *{{ $assertName }}) Has{{ .Name }}With(subAssert *{{ TrimPrefix (TypeWithoutPointerAndBrackets .ConcreteType) "sdk." }}Assert) *{{ $assertName }} {
        for _, assertion := range subAssert.GetAssertions() {
            assertion := assertion
            {{ $assertVar }}.AddAssertion(func(t *testing.T, o *{{ $sdkType }}) error {
                return assertion(t, &o.{{ .Name }})
            })
        }
        return {{ $assertVar }}
    }
{{- end }}
{{- else }}
    {{ $itemType := TypeWithoutPointerAndBrackets .ConcreteType }}
    func ({{ $assertVar }} *{{ $assertName }}) Has{{ .Name }}(expected {{ if .IsOriginalTypeSlice }}...{{ end }}{{ $itemType }}) *{{ $assertName }} {
        {{ $assertVar }}.AddAssertion(func(t *testing.T, o *{{ $sdkType }}) error {
            t.Helper()
            {{ if .IsOriginalTypeSlice -}}
            mapped := collections.Map(o.{{ .Name }}, func(item {{ $itemType }}) any { return {{ RunMapper .Mapper "item" }} })
            mappedExpected := collections.Map(expected, func(item {{ $itemType }}) any { return {{ RunMapper .ExpectedValueMapper "item" }} })
            if !slices.Equal(mapped, mappedExpected) {
                return fmt.Errorf("expected {{ CamelToWords .Name }}: %v; got: %v", expected, o.{{ .Name }})
            }
            {{ else -}}
            {{ if .IsOriginalTypePointer -}}
            if o.{{ .Name }} == nil {
                return fmt.Errorf("expected {{ CamelToWords .Name }} to have value; got: nil")
            }
            {{ end -}}
            {{- if .ComparisonFunc -}}
            if !{{ .ComparisonFunc }}({{ RunMapper .Mapper "o." .Name }}, {{ RunMapper .ExpectedValueMapper "expected" }}) {
                return fmt.Errorf("expected {{ CamelToWords .Name }}: %v; got: %v", {{ RunMapper .ErrorExpectedValueMapper "expected" }}, {{ RunMapper .ErrorMapper "o." .Name }})
            }
            {{- else -}}
            if {{ RunMapper .Mapper "o." .Name }} != {{ RunMapper .ExpectedValueMapper "expected" }} {
                return fmt.Errorf("expected {{ CamelToWords .Name }}: %v; got: %v", {{ RunMapper .ExpectedValueMapper "expected" }}, {{ RunMapper .Mapper "o." .Name }})
            }
            {{- end }}
            {{ end -}}
            return nil
        })
        return {{ $assertVar }}
    }
{{- end }}
{{- end }}
{{ end }}
